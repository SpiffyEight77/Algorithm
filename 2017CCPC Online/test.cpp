#include<iostream>
#include<cstdio>
#include<cstring>
#include<cmath>
#include<algorithm>
using namespace std;
const int M = 3300;
int sum,maxn,ans,a[M],num[M];
int n,m,T;
bool f;
int main()
{
    scanf("%d",&T);
    while(T--)
    {
        scanf("%d",&n);
        for (int i = 1; i <= n-1; i++)
        {
            for (int i = i+1; i <= n; i++)
            {
                scanf("%d",&x);
                a[i] 
            }
        }
    }
    return 0;
}