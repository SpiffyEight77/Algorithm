#include<iostream>
#include<map>
#include<set>
using namespace std;
int main()
{
    while(scanf("%d%d",&n,&m)!=EOF)
    {
        if(n == 0 && m == 0)
            break;
        map<>
        for (int i = 0; i < n; i++)
        {

        }
    }
    return 0;
}